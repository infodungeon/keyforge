id: no-primitive-index
message: "Primitive Obsession: Use KeyIndex/RowIndex/ColIndex domain types instead of raw usize/isize/i32 for indices."
severity: warning
language: rust
rule:
  kind: field_declaration
  all:
    - has:
        kind: field_identifier
        regex: "^(index|idx|row|col)$"
    - has:
        kind: primitive_type
        regex: "^(usize|i32|u32)$"
ignores:
  - "**/tests.rs"
  - "**/tests/**"
  - "**/generated/**"
  - "libs/keyforge-persistence/src/**/*.rs" # Storage often uses raw indices
