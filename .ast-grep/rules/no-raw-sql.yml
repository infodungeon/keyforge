id: no-raw-sql
message: "ARCH-004: All database interactions must use sqlx macros (query! or query_as!) for compile-time verification. Raw strings are banned."
severity: error
language: rust
rule:
  all:
    - any:
        - pattern: "sqlx::query($$$)"
        - pattern: "sqlx::query_as($$$)"
        - pattern: "sqlx::query_scalar($$$)"
    - not:
        inside:
          kind: mod_item
          has:
            kind: attribute
            pattern: "#[cfg(test)]"
ignores:
  - "**/tests/**"
  - "**/tests.rs"
  - "ops/repros/**"
  - "**/migrations/**"
