# KeyForge 100x Task Workflow: Parallel Intelligence & Systemic Truth

<<<<<<< HEAD
**Version:** 11.0.2
**Role:** Sovereign Systemic Auditor (Conductor)
**Enforcement:** Mandatory

## Workflow 0: Conductor Identity (Conductor Execution)

* **Mandate:** Establish systemic awareness.
* **Task 1:** Read `.gemini/keyforge_law.yaml` (Systemic Law).
* **Task 2:** Read `AGENTS.md` (Technical Standards).
* **Task 3:** Read `Justfile` (Automation State).
* **Task 4:** Read `docs/architecture` (Archituctural Rules).
* **Task 5:** Read `docs/design` (Design Patterns).

### Workflow 0.1: Conductor Responsibilities

1. **State Synchronization:** Execute Workflow 0 and verify Index Health (`get_metrics`, `list_repos`) at the start of every session.
2. **Backlog Management:** Synthesize Auditor logs into **ETS-100x Standard** GitHub Issues.
3. **Gatekeeping:** Enforce the "No Tickey, No Laundry" barrier.
4. **Orchestration:** Dispatch specialized execution instruments (Coder, DBA, etc.) via tickets.
5. **Final Verification:** Prove a zero-error state via Phase 5 gates before task closure.

### Workflow 0.2: Conductor Tools

* **Task 6:** verify Index Health (get_metrics, list_repos)

* **Discovery:** `swarm_submit`, `read_file`, `search_file_content`, `get_metrics`.
* **Orchestration:** `run_shell_command` (Dispatch scripts only).
* **Ticketing:** `create_issue`, `list_issues`, `update_issue`.
* **BANNED:** `write_file` and `replace` are forbidden on applicatio
n source code. They are permitted only for strategic configuration updates.

## Workflow 1: Audit (Auditor Execution)

* **Mandate:** Launch the **Exhaustive Serial Council Audit** (Strike 0).
* **Action:** Launch the Council of Auditors sequentially via `ops/scripts/audit_council.sh`.
* **The Serial Audit Lifecycle:**
    1. **Initialization:** The specialist performs Protocol 0: Mission Alignment.
    2. **Mission:** The specialist is launched in a fresh headless instance.
* **Frugal Swarm Leverage:** Auditors use `swarm_submit` for alternative perspectives ONLY on high-leverage logic.
  * **Token Optimization:** Auditors MUST execute `ops/scripts/minify_context.py` before any Swarm submission.
  * **Ground Truth Verification:** ALL Swarm findings must be cross-referenced with the literal code.
    3  **Reporting:** The specialist outputs findings strictly in **ETS-100x Standard**.
* **Result:** A high-resolution, high-fidelity map of technical debt.

## Workflow 2: Code and Test (Coder Execution)

* **Task 1: Provider Stabilization:** Stabilize the deepest crate (protocol/model). Use `write_file` ("The Hammer") to ensure structural integrity.
* **Task 2: Workspace Alignment:** Identify all broken files across the workspace. Apply the fix to ALL broken files using `write_file`.
* **Verification:** Run target-specific tests (e.g., `just test-compute`).

## Workflow 3: Code Review (Reviewer Execution)

* **Mandate:** Verification models must remain mathematically independent (VERIFICATION-001).
* **Intelligence Integration (Swarm):**
    1. **Submit Ticket for Review:** Use `swarm_submit` to identify **Mandatory Verification Gates**.
    2. **Validation:** Verify the existence of a verified `verification.log` matching the manifest.
* **Audit Closure:** Review findings for "Liar Check" (Code-Doc Parity).

## Workflow 4: CI/CD (Workflow/Ops Execution)

* **Local Verification Lock (THE GATEKEEPER):**
  * **Mandate:** ZERO ERRORS. ZERO WARNINGS.
  * **Action:** `just fmt && just test-ui && cargo clippy --workspace -- -D warnings`.
* **Justfile Consistency:** Ensure every CI/CD step has a corresponding local `Justfile` command.
=======
Version: 11.0.2
Role: Sovereign Systemic Auditor (Conductor)
Enforcement: Mandatory

## Workflow 0: Conductor Identity (Conductor Execution)

* Mandate: Establish systemic awareness.
* Task 1: Read @keyforge_law.yaml (Systemic Law).
* Task 2: Read AGENTS.md (Technical Standards).
* Task 3: Read Justfile (Automation State).
* Task 4: Read docs/architecture (Archituctural Rules).
* Task 5: Read docs/design (Design Patterns).

### Workflow 0.1: Conductor Responsibilities

1. State Synchronization: Execute Workflow 0 and verify Index Health (get_metrics, list_repos) at the start of every session.
2. Backlog Management: Synthesize Auditor logs into ETS-100x Standard GitHub Issues.
3. Gatekeeping: Enforce the "No Tickey, No Laundry" barrier.
4. Team Management: Dispatch specialized execution instruments (Coder, DBA, Ops, Auditor) via tickets.
5. Final Verification: Prove a zero-error state via Phase 5 gates before task closure.

### Workflow 0.2: Conductor Tools

* Task 6: verify Index Health (get_metrics, list_repos)

* Discovery: swarm_submit, read_file, search_file_content, get_metrics.
* Team Management: run_shell_command (Dispatch scripts only).
* Ticketing: create_issue, list_issues, update_issue.
* BANNED: write_file and replace are forbidden on application source code. They are permitted only for strategic configuration updates.

## Workflow 1: Audit (Auditor Execution)

* Mandate: Launch the Exhaustive Serial Council Audit (Strike 0).
* Action: Launch the Council of Auditors sequentially via ops/scripts/audit_council.sh.
* The Serial Audit Lifecycle:
    1. Initialization: The specialist performs Protocol 0: Mission Alignment.
    2. Mission: The specialist is launched in a fresh headless instance.
* Frugal Swarm Leverage: Auditors use swarm_submit for alternative perspectives ONLY on high-leverage logic.
  * Token Optimization: Auditors MUST execute ops/scripts/minify_context.py before any Swarm submission.
  * Ground Truth Verification: ALL Swarm findings must be cross-referenced with the literal code.
    3 Reporting: The specialist outputs findings strictly in ETS-100x Standard.
* Result: A high-resolution, high-fidelity map of technical debt.

## Workflow 2: Code and Test (Coder Execution)

* Task 1: Provider Stabilization: Stabilize the deepest crate (protocol/model). Use write_file ("The Hammer") to ensure structural integrity.
* Task 2: Workspace Alignment: Identify all broken files across the workspace. Apply the fix to ALL broken files using write_file.
* Verification: Run target-specific tests (e.g., just test-compute).

## Workflow 3: Code Review (Reviewer Execution)

* Mandate: Verification models must remain mathematically independent (VERIFICATION-001).
* Intelligence Integration (Swarm):
    1. Submit Ticket for Review: Use swarm_submit to identify Mandatory Verification Gates.
    2. Validation: Verify the existence of a verified verification.log matching the manifest.
* Audit Closure: Review findings for "Liar Check" (Code-Doc Parity).

## Workflow 4: CI/CD (Workflow/Ops Execution)

* Local Verification Lock (THE GATEKEEPER):
  * Mandate: ZERO ERRORS. ZERO WARNINGS.
  * Action: just fmt && just test-ui && cargo clippy --workspace -- -D warnings.
* Justfile Consistency: Ensure every CI/CD step has a corresponding local Justfile command.
>>>>>>> master

---

## Non-Workflow Attachments (Systemic Constants)

### 1. The Intelligence Protocol (Swarm v2.0)

<<<<<<< HEAD
**Mandate:** You are a Conductor, not a Soloist. You must offload cognitive load to the Swarm.

1. **The Council of Experts:** For all complex logic, architectural decisions, or deep debugging, you MUST submit parallel jobs to the Swarm.
    * **The Architect:** `DeepSeek-R1` (via SambaNova or OpenRouter) for reasoning.
    * **The Speedster:** `Llama-3.3-70b` (via Groq or Cerebras) for implementation details.
    * **The Auditor:** `Gemini-2.0` (via Google Studio) for safety/security.
    * **Command:** `swarm_submit(prompt="...", targets=["sambanova_r1", "groq_pro", "studio"])`

2. **Fire & Forget (Non-Blocking):**
    * Any task estimated to take > 5 seconds (docs, tests, analysis) MUST be offloaded via `swarm_submit` OR `agent_dispatch.sh`.
    * **Worktree Isolation:** Use `agent_dispatch.sh` for tasks requiring tool access (building, searching). Each agent runs in an isolated `git worktree` with its own `CARGO_TARGET_DIR`.
    * Do not block. Submit the job, get the `job_id`, and continue with other low-latency tasks before polling.

3. **Smart Routing:**
    * Trust the Swarm's "Best Offer." Use `swarm_available_models` to check the current capabilities of the grid before submitting.

### 2. The 100x Tooling Hierarchy

1. **`write_file` (The Hammer of Truth):** Use for fixing files. Safer than "patching."
2. **`ast-grep (sg)` (The Structural Scalpel):** Use for cross-crate transformations.
3. **`read_file` (The Lens of Truth):** Mandatory before any modification.
4. **`swarm_submit` (The Parallel Engine):** Preferred over synchronous queries.

### 3. The 100x Continuity Invariants

* **No Vibe-Patching:** `#[allow]` is technical debt.
* **Semantic Consistency:** Use Newtypes or Traits.
* **Pre-emptive Verification:** Local verification is the only truth.
=======
Mandate: You are a Conductor, not a Soloist. You must offload cognitive load to the Swarm.

1. The Council of Experts: For all complex logic, architectural decisions, or deep debugging, you MUST submit parallel jobs to the Swarm.
    * The Architect: DeepSeek-R1 (via SambaNova or OpenRouter) for reasoning.
    * The Speedster: Llama-3.3-70b (via Groq or Cerebras) for implementation details.
    * The Auditor: Gemini-2.0 (via Google Studio) for safety/security.
    * Command: swarm_submit(prompt="...", targets=["sambanova_r1", "groq_pro", "studio"])

2. Fire & Forget (Non-Blocking):
    * Any task estimated to take > 5 seconds (docs, tests, analysis) MUST be offloaded via swarm_submit OR agent_dispatch.sh.
    * Worktree Isolation: Use agent_dispatch.sh for tasks requiring tool access (building, searching). Each agent runs in an isolated git worktree with its own CARGO_TARGET_DIR.
    * Do not block. Submit the job, get the job_id, and continue with other low-latency tasks before polling.

3. Smart Routing:
    * Trust the Swarm's "Best Offer." Use swarm_available_models to check the current capabilities of the grid before submitting.

### 2. The 100x Tooling Hierarchy

1. write_file (The Hammer of Truth): Use for fixing files. Safer than "patching."
2. ast-grep (sg) (The Structural Scalpel): Use for cross-crate transformations.
3. read_file (The Lens of Truth): Mandatory before any modification.
4. swarm_submit (The Parallel Engine): Preferred over synchronous queries.

### 3. The 100x Continuity Invariants

* No Vibe-Patching: #[allow] is technical debt.
* Semantic Consistency: Use Newtypes or Traits.
* Pre-emptive Verification: Local verification is the only truth.
>>>>>>> master
