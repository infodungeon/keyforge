description = "Execute 100x workflow with async feedback loops."

prompt = """
Execute 100x orchestration for: {{args}}

### Step 1: Reconcile Background State
!{
    ./ops/scripts/reconcile_tasks.sh
}

### Step 2: Strategic Implementation
1. **Plan & Scout**: Use `codebase_investigator` and Copilot in parallel.
2. **Audit**: Verify against the Law (ast-grep).
3. **Implement**: Code changes with Correct-by-Construction patterns.

### Step 3: Dispatch Verification (Background)
!{
    ./ops/scripts/dispatch_task.sh "LocalVerify" "cargo test --workspace --limit-test-time"
}

### Step 4: GitHub Sync
Draft PR and monitor cloud feedback using `gh run watch`.
"""
